[package]
name = "motd"
version = "0.1.0"
edition = "2024"

[dependencies]
hex = {path="../hex"}
# Keep our crate debuggable, but build heavy deps (bevy) optimized.
[profile.dev]
opt-level = 1

[profile.dev.package."*"]
opt-level = 3
